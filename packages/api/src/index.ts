import { getDemoDashboardData } from "@forgeos/shared";

/** Dashboard API handlers — replace with Supabase queries in M1. */
export function fetchDashboardSummary() {
  return getDemoDashboardData();
}
