# ForgeOS 0.2.0 release gate report

**Date:** 2026-07-05  
**Branch:** `release/forgeos-0.2.0-local-demo`  
**Validator:** Composer (local clean install + automated gates)  
**Target version:** `0.2.0`

## Gate summary

| Gate | Result | Evidence |
|------|--------|----------|
| Deterministic demo seed | **PASS** | `src/demo/local-demo-lifecycle.test.ts` (idempotent seed, manifest) |
| Demo reset | **PASS** | Lifecycle tests + walkthrough workflow D |
| Backup and restore | **PASS** | Lifecycle tests + walkthrough workflow D |
| Integrated demo walkthrough | **PASS** | `test:demo-walkthrough` — 4/4 workflows |
| Unit tests | **PASS** | 421 passed, 3 skipped (80 files) |
| E2E tests | **PASS** | 61 passed (`forgeos-0.2.0-demo.spec.ts` excluded from default harness; run via walkthrough config) |
| Acceptance tests | **PASS** | 61 passed, 1 skipped (live-ai optional) |
| Production build | **PASS** | `npm run build` |
| `validate:security` | **PASS** | 0 high/critical (`npm audit --audit-level=high`; 2 moderate in exceljs/uuid chain) |
| `validate:full` | **PASS** | lint (16 warnings), typecheck, test, e2e, acceptance, build |
| `validate:release` | **PASS** | lifecycle + walkthrough + demo smoke harness |
| Demo smoke (Playwright) | **PASS** | `playwright.demo.config.ts` — 4/4 navigation tests |
| Console errors (critical routes) | **PASS** | `10-demo-0.2.0-critical-routes.spec.ts` + `08-controls-and-console-audit.spec.ts` |
| No external network (demo mode) | **PASS** | `attachPaidCallGuard` on walkthrough; demo contract clears hosted keys |
| No real email | **PASS** | `EMAIL_DELIVERY_PROVIDER=simulation` in demo contract |
| No paid AI | **PASS** | `AI_*_PROVIDER=deterministic` |
| No hosted Supabase mutation | **PASS** | `local-demo-guard.ts`; empty Supabase env in demo contract |
| Dedicated demo database | **PASS** | `forgeos:jhgomes:0.2.0-demo` |
| Working tree private data | **PASS** | Synthetic fixtures only; no `.env` committed |
| CI | **PENDING** | Awaiting push; workflow uses Node 22 on `release/**` |

## Local validation environment

| Item | Value |
|------|-------|
| OS | Windows 10 |
| Node (local) | v25.1.0 (EBADENGINE warning; Playwright gates pass) |
| npm (local) | 11.6.2 (corepack unavailable; `npm ci` succeeded) |
| CI Node | 22.x per `.github/workflows/ci.yml` |
| Clean install | `Remove-Item node_modules` + `npm ci` |

**Note:** `npm run demo:prepare`, `demo:start`, `demo:reset`, and `demo:smoke` CLI wrappers require **Node 22.x**. On Node 25, use `validate:release` (Playwright harness) or CI for equivalent smoke coverage.

## Seed manifest (`SEED_VERSION` 5)

| Entity | Count |
|--------|------:|
| Seed leads | 9 |
| Seed campaigns | 4 |
| Customers | 3 |
| Opportunities | 3 |
| Quotes | 3 |
| Production orders | 3 |
| Campaign recipients | 4 |
| Outreach messages | 3 |
| Send jobs | 1 |
| Customizer simulations | 1 |
| Email suppressions | 1 |

## Commands executed

```powershell
Remove-Item -Recurse -Force node_modules
npm ci
npm run validate:security
npm run validate:full          # e2e fixed: exclude walkthrough from default config
npm run test:acceptance
npm run build
npm run validate:release
```

## Excluded features (0.2.0)

- Hosted Supabase persistence and OAuth
- Brevo / Outlook live send
- Paid AI (Abacus, OpenAI)
- Maintenance CMMS operations
- Preview-only modules (sales orders, molds, quality)
- pt-PT diacritics visual polish (FORGE-QA-001, non-blocking)

## Version bump

Applied after all gates passed:

- `package.json` / `package-lock.json` → `0.2.0` via `npm version 0.2.0 --no-git-tag-version`
- `APP_VERSION` in `src/domain/constants.ts` → `0.2.0`
- Settings → **Sobre o ForgeOS** shows version, schema, seed, database

## Manual tag (post-approval)

```bash
git tag -a v0.2.0 -m "ForgeOS 0.2.0 local demo"
git push origin v0.2.0
```
