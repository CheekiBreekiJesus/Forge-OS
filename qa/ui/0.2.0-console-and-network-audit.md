# ForgeOS 0.2.0 console and network audit

**Date:** 2026-07-05  
**Branch:** `release/forgeos-0.2.0-local-demo`  
**Method:** Playwright `createConsoleCollector` + `attachPaidCallGuard` on acceptance runs

## Console audit (critical routes × 6 viewports)

**Spec:** `e2e/acceptance/10-demo-0.2.0-critical-routes.spec.ts`  
**Result:** **Pass** — no unexpected `console.error` or `pageerror` during route traversal at any viewport.

Collector rules (from `e2e/helpers/console-audit.ts`):

- Fails on `console.error` and uncaught page errors
- Ignores known Next.js dev hydration / HMR noise where configured

## Network audit

**Guard:** `attachPaidCallGuard` blocks outbound calls to paid AI / Brevo endpoints during acceptance.

| Check | Result |
|-------|--------|
| Paid AI provider calls | None observed (guard would fail test) |
| Brevo / external email | None in local demo acceptance |
| Failed fetch to app origin | None on critical route matrix |
| Supabase in acceptance mode | Stubbed local persistence only |

## Related acceptance suites (full run)

| Suite | Result |
|-------|--------|
| `npm run test:acceptance` | 61 passed, 1 skipped (includes new 0.2.0 spec) |
| `npm run test:e2e` | 61 passed |
| `08-controls-and-console-audit.spec.ts` | Pass (pre-existing console sweep) |

## Stale data / navigation

| Scenario | Result |
|----------|--------|
| LeadOps after demo reset | Fixed — campaigns reload on `dataVersion` bump |
| Route-to-route navigation | No console failures in matrix |

## Deferred manual checks

- Interactive `npm run demo:start` on Node 22 (blocked on CI/dev machine Node v25.1.0)
- Network tab review during Cup Customizer image/logo flows (deterministic providers in demo)
