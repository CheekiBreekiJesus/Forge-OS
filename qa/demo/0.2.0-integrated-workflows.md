# ForgeOS 0.2.0 integrated workflows validation

**Date:** 2026-07-05  
**Branch:** `release/forgeos-0.2.0-local-demo`  
**Database:** `forgeos:jhgomes:0.2.0-demo`  
**Spec:** `e2e/forgeos-0.2.0-demo.spec.ts`  
**Config:** `playwright.demo-walkthrough.config.ts` (port 3003)

## Summary

Four end-to-end local demo workflows validated via Playwright with deterministic data, paid-call guard, and isolated IndexedDB reset per test.

| Workflow | Result | Notes |
|----------|--------|-------|
| A — Commercial outreach | **Pass** | Import → campaign → drafts → approve → simulate send → suppression → reload |
| B — Cup Customizer | **Pass** | Artwork → mockup → quote → reopen → approve → production (no duplicate quote/PO) |
| C — Customer to production | **Pass** | Guided `/pt-PT/demo` CRM→production; PO visible; EN locale smoke |
| D — Local operations | **Pass** | Profile, sender, backup/restore, demo reset, deterministic restore |

## Integration defects fixed

| ID | Workflow | Issue | Fix |
|----|----------|-------|-----|
| INT-0.2.0-001 | B | No UI path from approved customizer quotation to production order | `quotations-shell.tsx`: row action **Criar ordem de produção** via `createFromQuote`; feedback + idempotent disable |
| INT-0.2.0-002 | A (test) | Suppression after completed send job left batch button disabled | Test order: queue → suppress → process (matches `campaign-send-job-simulation.spec.ts`) |

## Persistence and idempotency

| Check | Result |
|-------|--------|
| Reload after outreach campaign | Pass — campaign + send job panel persist |
| Reload after customizer quote | Pass — quote number stable; single row after re-convert |
| Duplicate production order | Pass — second create disabled; repository duplicate guard |
| Demo backup restore | Pass — company name restored from JSON backup |
| Deterministic reseed | Pass — `leadops_001` outreach + LeadOps table after restore |

## Tenant and IDs

- Backup exports `tenantId: tenant_jh_gomes`
- Walkthrough uses synthetic `@example.invalid` emails only
- Production numbers use `PO-#####` format

## External-call guard

- `attachPaidCallGuard` on all workflows — **no paid AI host requests**
- Outreach generation asserts `deterministic` provider via `/api/leadops/generate`
- Send job panel shows simulation-only banners; no Brevo queue controls

## Console audit

All four workflows: **clean** (`createConsoleCollector.assertClean()`).

## Commands

| Command | Result |
|---------|--------|
| `npm run test:demo-walkthrough` | **4 passed** (37s) |
| `npx playwright test --config playwright.demo-walkthrough.config.ts` | **4 passed** |
| `npm run demo:reset` | **Blocked** — Node v25.1.0 (requires Node 22.x) |
| `npm run demo:seed` | **Blocked** — Node v25.1.0 |
| `npm run demo:smoke` | **Blocked** — Node v25.1.0 |

Walkthrough tests perform equivalent reset via `resetDemoWalkthroughState()` (clears demo IndexedDB before each workflow).

## Remaining limitations

- `npm run demo:*` CLI scripts require Node 22 on this machine; CI/customer PC should use Node 22 per `package.json` engines
- Interactive `demo:start` on port 3000 not exercised here (walkthrough uses Playwright webServer on 3003)
- pt-PT diacritics polish (FORGE-QA-001) still deferred

## Files added/changed

- `e2e/forgeos-0.2.0-demo.spec.ts`
- `e2e/helpers/demo-walkthrough.ts`
- `playwright.demo-walkthrough.config.ts`
- `src/components/quotations-shell.tsx` (production from quote action)
- `package.json` (`test:demo-walkthrough` script)
